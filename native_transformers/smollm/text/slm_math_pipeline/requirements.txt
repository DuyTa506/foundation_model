# Core training stack
torch==2.7.0
torchaudio==2.7.0
torchvision==0.22.0
transformers
accelerate==1.7.0
datasets==3.6.0
trl>=0.15
peft

# Data curation (datatrove replaces the broken filter/dedup/prepare scripts)
datatrove[processing]          # streaming, sharded, restartable curation pipeline
spacy                          # word tokenizer backing datatrove's Gopher/quality filters
                               # (NOT pulled by datatrove[processing]; stage 01 ImportErrors without it)
fasttext                       # language ID + UltraClean quality classifier
huggingface_hub                # download GlotLID / Ultra-FineWeb-classifier
pyarrow==20.0.0

# Tokenizer training (from scratch)
tokenizers                     # byte-level BPE; exports HF-compatible tokenizer.json

# Math reward verification (GRPO)
sympy                          # symbolic math equivalence for correctness reward

# Other utilities
numpy==1.26.4
scipy==1.15.3
scikit-learn==1.7.0
sentencepiece==0.2.0
safetensors==0.5.3
pyyaml
tqdm
wandb
tensorboard                    # alternative to wandb; set report_to: tensorboard in config
bitsandbytes
deepspeed

# Evaluation
lighteval[extended_tasks,multilingual] @ git+https://github.com/huggingface/lighteval.git@2788496c3ef9973a24c7d8b87ee5b2cceb430872
vllm==0.9.1

# Removed: langdetect (replaced by fasttext + GlotLID in curate/02_language_id.py)
# Retained for compatibility: the old scripts still import it as a fallback.
langdetect==1.0.9
