# Minimal direct dependencies for the SLM pipeline.
#
# Let uv resolve compatible transitive versions for the target server instead
# of pinning stale local sub-dependencies. Target server observed:
# NVIDIA driver/KMD 610.43.02, CUDA UMD 13.3, H200/Hopper.
#
# Keep lighteval separate in requirements-eval.txt because its current dev
# snapshot pins numpy<2 and conflicts with the training/vLLM stack.

# H200 training / inference anchor. vLLM pulls its compatible torch/CUDA stack.
vllm==0.22.1

# Core training / HF stack
# transformers 5.10.2 ships a new TokenizersBackend loader that is incompatible
# with tokenizers 0.22.x (saved special tokens reload as a list, not a dict,
# crashing PreTrainedTokenizerFast.from_pretrained). Pin the known-good pair.
transformers==5.10.2
accelerate
datasets
safetensors
tokenizers==0.21.4

# Scale training / post-training
trl
peft
deepspeed
bitsandbytes
wandb
tensorboard

# Data curation
datatrove[processing]
spacy
fasttext-numpy2-wheel
pyarrow

# Tokenizer / math / utility stack
numpy
sentencepiece
sympy
pyyaml
tqdm

# Retained for compatibility with older scripts/fallback paths.
langdetect
